# SurelyPlaced Knowledge OS — Environment Configuration Template
# DO NOT place actual secrets in this file. Copy to .env.local for local development.

# Neon Postgres Database Connection String (Server-side only)
DATABASE_URL=

# AI Provider Selection (mock | production)
COPILOT_AI_PROVIDER=mock
